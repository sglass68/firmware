[Hook Scripts]
hook0 = (cd unittests && ./crosutil_unittest) 2>&1
hook1 = if echo ${PRESUBMIT_FILES} | grep -q pack_firmware.sh; then echo "Please don't check in changes to pack_firmware.sh. Use pack_firmware.py instead."; exit 1; fi
